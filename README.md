# Delivery App
Uses vite + react + typescript + cypress

Github pages url
https://canbakis.github.io/delivery-app/

## Build & Run
`npm run build`  
`npm run preview`

## Build & Run Tests
`npm run build`  
`npm run preview`
`npm run cpress`